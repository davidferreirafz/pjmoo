class PathUpLeft
!!!172510.cpp!!!	PathUpLeft()

!!!172638.cpp!!!	~PathUpLeft()

!!!172766.cpp!!!	mover(in velocidade : int, inout posicao : Ponto, in dimensao : Dimensao) : bool

	posicao->x-=velocidade;
	posicao->y-=velocidade;	
	
    if ((posicao->y<area.top-dimensao.h)||(posicao->x<area.left-dimensao.w)){
		return true;
	} else {
		return false;
	}
